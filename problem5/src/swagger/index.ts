import {
  productSwagger,
  productSchemas,
} from '@modules/products/product.swagger';

export const swaggerPaths = {
  ...productSwagger,
};

export const swaggerSchemas = {
  ...productSchemas,
};
